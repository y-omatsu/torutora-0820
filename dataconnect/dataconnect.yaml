specVersion: "v1"
serviceId: "project"
location: "asia-northeast1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "project-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
